# TODO for myOrders.ejs CSS Update

- [x] Add timeline HTML inside each .order-card after the action section, with steps based on order status.
- [x] Modify .order-card CSS: remove card styling (background, shadow, border-radius), make it flex row with gap, add border-bottom for separation.
- [x] Adjust .item-box CSS: make it flex row, details on left, larger image on right.
- [x] Style the timeline at the bottom of each order.
- [x] Ensure responsive design for mobile (stack vertically on small screens).
- [x] Test layout on different screen sizes and ensure images are larger and load properly.
